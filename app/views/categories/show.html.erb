<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Privacy:</strong>
  <%= @category.public ? "Public" : "Private" %>
</p>

<p>
  <strong>Category:</strong>
  <%= @category.category.nil? ? "General" : @category.category.name.capitalize %>
</p>

<%= link_to 'Edit', edit_category_path(@category), class: 'btn btn-primary mt-2' %> |
<%= link_to 'Back', categories_path, class: 'btn btn-primary mt-2' %>
